
export function renderResourceCard(resourceURL, resourceType) {
    console.log(resourceURL);
    console.log(resourceType);
    // Cada recurso se renderiza de manera diferente. Esta funcion invoca al componente correspondiente:
    // ie: 
    /*  renderPokemonCard(resourceURL);
        renderBerryCard(resourceURL);
        renderItemCard(resourceURL);
        renderMoveCard(resourceURL);
    */
}
